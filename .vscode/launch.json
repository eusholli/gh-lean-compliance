{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Node: Current File (Integrated Terminal)",
      "type": "node",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "cwd": "${fileDirname}"
    }
  ]
}
